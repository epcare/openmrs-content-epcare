{"uuid":"06679ecb-96b5-4800-962d-000d89378a9c","name":"TX- RTT PIPS","description":"NA","creator":"82f18b44-6814-11e8-923f-e9a88dcb533f","dateCreated":1786167582000,"changedBy":"82f18b44-6814-11e8-923f-e9a88dcb533f","dateChanged":1789381820000,"retired":false,"dateRetired":null,"retiredBy":null,"retireReason":null,"id":278,"code":"TX__RTT_PIPS","kind":"CUSTOM","defaultValueType":"NUMBER","configJson":"{\n  \"version\": 1,\n  \"patientIdColumn\": \"client_id\",\n  \"populationQuery\": {\n    \"extractFrom\": \"sqlTemplate\"\n  },\n  \"supportsRedisaggregation\": true,\n  \"redisaggregationStrategy\": \"population-extraction\",\n  \"themeIndependent\": true,\n  \"sqlTemplate\": \"SELECT DISTINCT a.client_id AS patient_id\\nFROM (SELECT client_id,\\n             TIMESTAMPDIFF(\\n                     DAY,\\n                     MAX(return_visit_date),\\n                     :endDate\\n             ) AS ltfp_days\\n      FROM mamba_fact_encounter_hiv_art_card\\n      WHERE encounter_date &lt;= :endDate\\n        AND return_visit_date &gt;= :startDate\\n      GROUP BY client_id) a\\n\\n         LEFT JOIN (SELECT *\\n                    FROM person p\\n                    WHERE p.dead = 1\\n                      AND p.death_date &lt;= :endDate) p\\n                   ON a.client_id = p.person_id\\n\\n         LEFT JOIN (SELECT DISTINCT mf_to.client_id\\n                    FROM mamba_fact_transfer_out mf_to\\n                             LEFT JOIN mamba_fact_transfer_in mf_ti\\n                                       ON mf_to.client_id = mf_ti.client_id\\n                    WHERE mf_to.transfer_out_date &lt;= :endDate\\n                      AND (\\n                        mf_to.transfer_out_date &gt; mf_ti.transfer_in_date\\n                            OR mf_ti.client_id IS NULL\\n                        )) mfto\\n                   ON a.client_id = mfto.client_id\\n\\n         INNER JOIN (SELECT DISTINCT x.client_id\\n                     FROM (SELECT client_id,\\n                                  TIMESTAMPDIFF(\\n                                          DAY,\\n                                          MAX(return_visit_date),\\n                                          DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                                  ) AS ltfp_days\\n                           FROM mamba_fact_encounter_hiv_art_card\\n                           WHERE encounter_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                             AND return_visit_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                           GROUP BY client_id) x\\n                     WHERE x.ltfp_days &gt; 28) lost_in_previous_quarter\\n                    ON a.client_id = lost_in_previous_quarter.client_id\\n\\n         INNER JOIN (SELECT DISTINCT client_id\\n                     FROM mamba_fact_encounter_hiv_art_summary\\n                     WHERE special_category = 'In prison') special_category\\n                    ON a.client_id = special_category.client_id\\n\\nWHERE a.ltfp_days &lt;= 28\\n  AND p.person_id IS NULL\\n  AND mfto.client_id IS NULL;\",\n  \"sqlPreview\": \"SELECT DISTINCT a.client_id AS patient_id\\nFROM (SELECT client_id,\\n             TIMESTAMPDIFF(\\n                     DAY,\\n                     MAX(return_visit_date),\\n                     :endDate\\n             ) AS ltfp_days\\n      FROM mamba_fact_encounter_hiv_art_card\\n      WHERE encounter_date &lt;= :endDate\\n        AND return_visit_date &gt;= :startDate\\n      GROUP BY client_id) a\\n\\n         LEFT JOIN (SELECT *\\n                    FROM person p\\n                    WHERE p.dead = 1\\n                      AND p.death_date &lt;= :endDate) p\\n                   ON a.client_id = p.person_id\\n\\n         LEFT JOIN (SELECT DISTINCT mf_to.client_id\\n                    FROM mamba_fact_transfer_out mf_to\\n                             LEFT JOIN mamba_fact_transfer_in mf_ti\\n                                       ON mf_to.client_id = mf_ti.client_id\\n                    WHERE mf_to.transfer_out_date &lt;= :endDate\\n                      AND (\\n                        mf_to.transfer_out_date &gt; mf_ti.transfer_in_date\\n                            OR mf_ti.client_id IS NULL\\n                        )) mfto\\n                   ON a.client_id = mfto.client_id\\n\\n         INNER JOIN (SELECT DISTINCT x.client_id\\n                     FROM (SELECT client_id,\\n                                  TIMESTAMPDIFF(\\n                                          DAY,\\n                                          MAX(return_visit_date),\\n                                          DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                                  ) AS ltfp_days\\n                           FROM mamba_fact_encounter_hiv_art_card\\n                           WHERE encounter_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                             AND return_visit_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\\n                           GROUP BY client_id) x\\n                     WHERE x.ltfp_days &gt; 28) lost_in_previous_quarter\\n                    ON a.client_id = lost_in_previous_quarter.client_id\\n\\n         INNER JOIN (SELECT DISTINCT client_id\\n                     FROM mamba_fact_encounter_hiv_art_summary\\n                     WHERE special_category = 'In prison') special_category\\n                    ON a.client_id = special_category.client_id\\n\\nWHERE a.ltfp_days &lt;= 28\\n  AND p.person_id IS NULL\\n  AND mfto.client_id IS NULL;\"\n}","sqlTemplate":"SELECT DISTINCT a.client_id AS patient_id\nFROM (SELECT client_id,\n             TIMESTAMPDIFF(\n                     DAY,\n                     MAX(return_visit_date),\n                     :endDate\n             ) AS ltfp_days\n      FROM mamba_fact_encounter_hiv_art_card\n      WHERE encounter_date &lt;= :endDate\n        AND return_visit_date &gt;= :startDate\n      GROUP BY client_id) a\n\n         LEFT JOIN (SELECT *\n                    FROM person p\n                    WHERE p.dead = 1\n                      AND p.death_date &lt;= :endDate) p\n                   ON a.client_id = p.person_id\n\n         LEFT JOIN (SELECT DISTINCT mf_to.client_id\n                    FROM mamba_fact_transfer_out mf_to\n                             LEFT JOIN mamba_fact_transfer_in mf_ti\n                                       ON mf_to.client_id = mf_ti.client_id\n                    WHERE mf_to.transfer_out_date &lt;= :endDate\n                      AND (\n                        mf_to.transfer_out_date &gt; mf_ti.transfer_in_date\n                            OR mf_ti.client_id IS NULL\n                        )) mfto\n                   ON a.client_id = mfto.client_id\n\n         INNER JOIN (SELECT DISTINCT x.client_id\n                     FROM (SELECT client_id,\n                                  TIMESTAMPDIFF(\n                                          DAY,\n                                          MAX(return_visit_date),\n                                          DATE_SUB(:endDate, INTERVAL 3 MONTH)\n                                  ) AS ltfp_days\n                           FROM mamba_fact_encounter_hiv_art_card\n                           WHERE encounter_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\n                             AND return_visit_date &lt;= DATE_SUB(:endDate, INTERVAL 3 MONTH)\n                           GROUP BY client_id) x\n                     WHERE x.ltfp_days &gt; 28) lost_in_previous_quarter\n                    ON a.client_id = lost_in_previous_quarter.client_id\n\n         INNER JOIN (SELECT DISTINCT client_id\n                     FROM mamba_fact_encounter_hiv_art_summary\n                     WHERE special_category = 'In prison') special_category\n                    ON a.client_id = special_category.client_id\n\nWHERE a.ltfp_days &lt;= 28\n  AND p.person_id IS NULL\n  AND mfto.client_id IS NULL;","base":false,"custom":true,"composite":false,"kindLower":"custom","final":false}